# GUI
PyQt6==6.10.0
PyQt6-Qt6==6.10.0
PyQt6_sip==13.10.2
qdarkstyle>=3.0.0

# Core Processing
rembg[gpu,cli]==2.0.60
onnxruntime==1.24.1  # pinned for stability with rembg
Pillow>=9.0.0
numpy==1.26.4
opencv-python>=4.8.0

# Upscaling (Real-ESRGAN dependencies)
# torch e torchvision sao gerenciados pelo install.sh (pinados em 2.0.1/0.15.2)
# para compatibilidade com basicsr 1.4.2 — nao adicionar aqui para evitar
# conflito com o --index-url do install.sh
basicsr>=1.4.2
realesrgan>=0.3.0

# Video / Animation
imageio>=2.30.0
imageio-ffmpeg>=0.4.8

# Utilities
requests>=2.28.0
tqdm>=4.60.0
svgwrite>=1.4.3
svglib>=1.5.1
vtracer>=0.6.0

# Ferramentas de desenvolvimento estao em requirements-ci.txt
